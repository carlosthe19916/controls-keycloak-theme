{
  "name": "controls-keycloak-theme",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "keycloak:start": "docker run -p 8080:8080 -e KEYCLOAK_USER=admin -e KEYCLOAK_PASSWORD=admin -e KEYCLOAK_WELCOME_THEME=konveyor konveyor/controls-keycloak-theme:test"
  },
  "dependencies": {
    "cypress": "^6.2.1"
  }
}
